{
  "compilerOptions": {
    "baseUrl": ".",  // This sets the root directory as the base for path resolution.
    "paths": {
      "@/*": ["./src/*"]  // This tells the compiler that '@/' maps to the 'src' folder.
    }
  }
}
